export interface User {
  userId: string;
  email: string;
  name: string;
  password: string;
  createdAt: string;
}

export interface UserProfile {
  userId: string;
  email: string;
  name: string;
}
